/*
This has been generated by the overpass-turbo wizard.
The original search was:
“airport in "pulau kisar"”
*/
[out:json][timeout:25];
// fetch area “pulau kisar” to search in
area(2425870936)->.searchArea;
// gather results
(
  // query part for: “airport”
  node["aeroway"="aerodrome"](area.searchArea);
  way["aeroway"="aerodrome"](area.searchArea);
  relation["aeroway"="aerodrome"](area.searchArea);
);
// print results
out body;
>;
out skel qt;